on:
  push:
jobs:     
  job1:    
     name: job1
     runs-on: ubuntu-latest 
     steps:
      - name: step 1
        run: |
         pwd
         ls -ltrc
         hostname
      - name: step 2 - checkout
        uses: actions/checkout@v5.0.0
      - name: step 3
        run: |
         pwd
         ls -ltrc
         hostname
